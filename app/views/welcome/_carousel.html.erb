<div id="carousel-example-generic" class="carousel slide" data-ride="carousel" data-interval="4000" data-pause="">
  <!-- Indicators -->
  <% 
      first_img = @carousel_imgs.first
      num = @carousel_imgs.size
      i = 0
  %>
  <ol class="carousel-indicators">
    <% while i < num %>

      <li data-target="#carousel-example-generic" data-slide-to="<%= i %>" class="<%= 'active' if i == 0 %>">
      </li>
      <% i += 1 %>
    <% end %>
  </ol>

  <!-- Wrapper for slides -->
  <div class="carousel-inner" role="listbox">
    <% @carousel_imgs.each do |img| %>
      <div class="item <%= "active" if img.id == first_img.id %>">
        <img src="<%= img.url %>">
      </div>
    <% end %>
  </div>

</div>

